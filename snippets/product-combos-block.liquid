{% comment %}
  Product page – Cuadrado de Combos (productos con tag "combo").
  Muestra solo productos que tengan el tag "combo".
  Pass: collection (colección que puede contener combos, o usamos all_products no aplica – solo collection).
{% endcomment %}

{% assign combos_collection = collection | default: nil %}

{% comment %} Contar solo productos con tag "combo" (limit 50 si es colección grande tipo "all") {% endcomment %}
{% assign combo_products_count = 0 %}
{% if combos_collection != blank %}
  {% for product in combos_collection.products limit: 50 %}
    {% if product.tags contains 'combo' %}
      {% assign combo_products_count = combo_products_count | plus: 1 %}
    {% endif %}
  {% endfor %}
{% endif %}

{% if combos_collection != blank and combo_products_count > 0 %}
<div class="product-combos-box" id="product-combos-block">
  <div class="product-combos-box-inner">
    <h3 class="product-combos-title">Combos</h3>
    <p class="product-combos-subtitle">Choose a combo — each box is a variant option</p>
    <div class="product-combos-variants">
      {% assign shown = 0 %}
      {% for product in combos_collection.products limit: 50 %}
        {% if product.tags contains 'combo' and shown < 8 %}
          {% assign shown = shown | plus: 1 %}
          <a href="{{ product.url }}" class="combo-variant-cajita">
            <div class="combo-variant-image">
              {% if product.featured_image %}
                <img src="{{ product.featured_image | image_url: width: 300 }}" alt="{{ product.title | escape }}" loading="lazy" width="120" height="120">
              {% else %}
                <span class="combo-variant-placeholder">—</span>
              {% endif %}
            </div>
            <div class="combo-variant-info">
              <span class="combo-variant-name">{{ product.title }}</span>
              <span class="combo-variant-price">{{ product.price | money }}</span>
            </div>
          </a>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</div>
<style>
/* Contenedor combos */
.product-combos-box {
  margin-top: 32px;
  border: 2px solid rgba(236, 0, 140, 0.18);
  border-radius: var(--radius-lg, 16px);
  background: linear-gradient(135deg, rgba(236, 0, 140, 0.04) 0%, #fff 100%);
  overflow: hidden;
}
.product-combos-box-inner {
  padding: 20px;
}
.product-combos-title {
  font-size: 1.125rem;
  font-weight: 700;
  color: var(--text-dark, #1a1a1a);
  margin: 0 0 4px;
}
.product-combos-subtitle {
  font-size: 0.8125rem;
  color: var(--text-gray, #555);
  margin: 0 0 16px;
}
/* Grid de cajitas como variantes */
.product-combos-variants {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
}
@media (min-width: 400px) {
  .product-combos-variants {
    grid-template-columns: repeat(3, 1fr);
  }
}
@media (min-width: 640px) {
  .product-combos-variants {
    grid-template-columns: repeat(4, 1fr);
  }
}
/* Cajita = una variante (estilo tipo selector de tamaño) */
.combo-variant-cajita {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-decoration: none;
  color: inherit;
  border: 2px solid var(--light-gray, #ddd);
  border-radius: var(--radius-md, 12px);
  overflow: hidden;
  background: #fff;
  padding: 0;
  transition: border-color 0.2s ease, background 0.2s ease, box-shadow 0.2s ease;
  min-width: 0;
}
.combo-variant-cajita:hover {
  border-color: var(--primary-pink, #ec008c);
  background: var(--off-white, #fafafa);
  box-shadow: 0 2px 10px rgba(236, 0, 140, 0.12);
}
.combo-variant-image {
  width: 100%;
  aspect-ratio: 1;
  overflow: hidden;
  background: #f5f5f5;
}
.combo-variant-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}
.combo-variant-placeholder {
  width: 100%;
  height: 100%;
  min-height: 80px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24px;
  color: #bbb;
  background: #eee;
}
.combo-variant-info {
  padding: 10px 12px;
  width: 100%;
  text-align: center;
  display: flex;
  flex-direction: column;
  gap: 2px;
}
.combo-variant-name {
  font-size: 0.8125rem;
  font-weight: 600;
  color: var(--text-dark, #1a1a1a);
  line-height: 1.25;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
.combo-variant-price {
  font-size: 0.875rem;
  font-weight: 700;
  color: var(--primary-pink, #ec008c);
}
</style>
{% endif %}
