{% comment %}
  Product page – Cuadrado de Combos (productos con tag "combo").
  Muestra solo productos que tengan el tag "combo".
  Pass: collection (colección que puede contener combos, o usamos all_products no aplica – solo collection).
{% endcomment %}

{% assign combos_collection = collection | default: nil %}

{% comment %} Contar solo productos con tag "combo" {% endcomment %}
{% assign combo_products_count = 0 %}
{% if combos_collection != blank %}
  {% for product in combos_collection.products %}
    {% if product.tags contains 'combo' %}
      {% assign combo_products_count = combo_products_count | plus: 1 %}
    {% endif %}
  {% endfor %}
{% endif %}

{% if combos_collection != blank and combo_products_count > 0 %}
<div class="product-combos-box" id="product-combos-block">
  <div class="product-combos-box-inner">
    <h3 class="product-combos-title">Combos</h3>
    <p class="product-combos-subtitle">Save more with these combos</p>
    <div class="product-combos-grid">
      {% assign shown = 0 %}
      {% for product in combos_collection.products %}
        {% if product.tags contains 'combo' and shown < 6 %}
          {% assign shown = shown | plus: 1 %}
          <a href="{{ product.url }}" class="product-combos-card">
            <div class="product-combos-card-image">
              {% if product.featured_image %}
                <img src="{{ product.featured_image | image_url: width: 400 }}" alt="{{ product.title | escape }}" loading="lazy" width="200" height="200">
              {% else %}
                <div class="product-combos-card-placeholder">No image</div>
              {% endif %}
            </div>
            <div class="product-combos-card-info">
              <span class="product-combos-card-name">{{ product.title }}</span>
              <span class="product-combos-card-price">{{ product.price | money }}</span>
            </div>
          </a>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</div>
<style>
/* Cuadrado contenedor de combos */
.product-combos-box {
  margin-top: 32px;
  border: 2px solid rgba(236, 0, 140, 0.2);
  border-radius: var(--radius-lg, 16px);
  background: linear-gradient(135deg, rgba(236, 0, 140, 0.03) 0%, rgba(255,255,255,1) 100%);
  overflow: hidden;
}
.product-combos-box-inner {
  padding: 24px;
}
.product-combos-title {
  font-size: 1.25rem;
  font-weight: 700;
  color: var(--text-dark, #1a1a1a);
  margin: 0 0 4px;
}
.product-combos-subtitle {
  font-size: 0.875rem;
  color: var(--text-gray, #555);
  margin: 0 0 20px;
}
/* Grid de tarjetas cuadradas */
.product-combos-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 16px;
}
@media (min-width: 480px) {
  .product-combos-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}
/* Tarjeta: cuadrado (imagen + info) */
.product-combos-card {
  display: flex;
  flex-direction: column;
  text-decoration: none;
  color: inherit;
  border-radius: var(--radius-md, 12px);
  overflow: hidden;
  border: 1px solid rgba(236, 0, 140, 0.15);
  background: #fff;
  transition: box-shadow 0.2s ease, border-color 0.2s ease;
  aspect-ratio: 1;
  max-width: 200px;
  margin: 0 auto;
}
.product-combos-card:hover {
  border-color: rgba(236, 0, 140, 0.4);
  box-shadow: 0 4px 16px rgba(236, 0, 140, 0.15);
}
.product-combos-card-image {
  aspect-ratio: 1;
  overflow: hidden;
  background: #f5f5f5;
}
.product-combos-card-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}
.product-combos-card-placeholder {
  width: 100%;
  height: 100%;
  min-height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  color: #999;
  background: #eee;
}
.product-combos-card-info {
  padding: 12px;
  display: flex;
  flex-direction: column;
  gap: 4px;
  flex: 1;
  justify-content: center;
}
.product-combos-card-name {
  font-size: 0.875rem;
  font-weight: 600;
  color: var(--text-dark, #1a1a1a);
  line-height: 1.3;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
.product-combos-card-price {
  font-size: 0.9375rem;
  font-weight: 700;
  color: var(--primary-pink, #ec008c);
}
</style>
{% endif %}
